# Contract Examples

These files are intentionally small, hand-authored examples for the v1 UBC handoff.

Use the docs in `contracts/` and the copied Zod schemas in `typescript-schemas/` as the source of truth.
